package algorithms_datastructures.algorithms.sort;

/*Сложность: линейная
 * - для каждого значения создается типа корзины и потом проход по всем элементам, раскладывая их в
 * соотв. корзины (инкрементируя количество в корзине)
 * - потом из корзин восстанавливается массив
 * - но если всего например 100 элементов, при этом максимальные значения элементов напр. миллион,
 * то уже не так практичен
 *
 * */

public class CountingSort {
}
