package __algorithms_datastructures.algorithms.sort;

/* сложность линейнологарифмическая
 * - используется структура хип
 * - корень меняется местом с последним элементом, а потом производится починка хипа (т.е. bubbling)
 * - потом корень меняется место с предпоследним и фиксится, и т. далее
 *
 * */

public class HeapSort {
}
